{
  "schema_version": "SHDHybridCartesianMemberScaling/v1",
  "selection": {
    "report": "artifacts/shd_v71_cartesian_development_selection_canonical_v1/cartesian_development_selection.json",
    "field": "selected_input",
    "split": "repair_calibration",
    "uses_labels": false
  },
  "family": {
    "relative_timestep_radius": 0.01,
    "relative_threshold_radius": 0.01,
    "integration_rules": ["forward_euler", "exponential_euler"],
    "threshold_timings": ["post_integration", "pre_integration"],
    "reset_rules": ["subtractive", "to_value"],
    "synaptic_delays": [0, 1],
    "output_delays": [0]
  },
  "certificate_budgets": {
    "maximum_polygon_leaves": [16, 64, 256],
    "maximum_local_branches": 64,
    "maximum_guard_band_splits": 8
  },
  "execution": {
    "worker_count": 16,
    "blas_threads_per_worker": 1
  },
  "interpretation": "Development-only member-wise resource scaling. Each mutually exclusive discrete semantics member is soundly analyzed on the same label-free selected calibration input so that Cartesian bottlenecks can be attributed before changing any scientific budget."
}
