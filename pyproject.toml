[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "transportcert"
version = "0.1.0"
description = "Transport certificates for finite-horizon digital spiking neural networks"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [{name = "Anonymous Authors"}]
dependencies = ["numpy>=2.0"]

[project.optional-dependencies]
dev = ["pytest>=8", "pytest-cov>=5"]
nir = ["nir>=1.0"]
torch = ["torch>=2.5"]

[project.scripts]
transportcert = "transportcert.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra --strict-markers"
